import { Drtv2Directive } from './drtv2.directive';

describe('Drtv2Directive', () => {
  it('should create an instance', () => {
    const directive = new Drtv2Directive();
    expect(directive).toBeTruthy();
  });
});
